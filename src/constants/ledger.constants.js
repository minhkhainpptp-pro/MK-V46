const AR_LEDGER_TYPE = Object.freeze({
  SALE: 'AR-SALE',
  RETURN: 'AR-RETURN',
  RECEIPT: 'AR-RECEIPT',
  BONUS: 'AR-BONUS',
  SALE_REVERSAL: 'AR-SALE-REV',
});

const FUND_LEDGER_TYPE = Object.freeze({
  CASH_RECEIPT: 'CASH_RECEIPT',
  BANK_RECEIPT: 'BANK_RECEIPT',
  CASH_PAYMENT: 'CASH_PAYMENT',
  BANK_DEPOSIT: 'BANK_DEPOSIT',
  BONUS_PAYMENT: 'BONUS_PAYMENT',
});

const INVENTORY_TRANSACTION_TYPE = Object.freeze({
  IN_IMPORT: 'IN_IMPORT',
  OUT_SALE: 'OUT_SALE',
  IN_RETURN: 'IN_RETURN',
  ADJUSTMENT: 'ADJUSTMENT',
});

const JOURNAL_TYPE = Object.freeze({
  SALE: 'SALE',
  RETURN: 'RETURN',
  RECEIPT: 'RECEIPT',
  INVENTORY: 'INVENTORY',
  ACCOUNTING_CONFIRM: 'ACCOUNTING_CONFIRM',
});

module.exports = {
  AR_LEDGER_TYPE,
  FUND_LEDGER_TYPE,
  INVENTORY_TRANSACTION_TYPE,
  JOURNAL_TYPE,
};
